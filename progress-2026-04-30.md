# 2026-04-30 进展

## Done

### v50 (479fb12)
- ✅ 四层 LaTeX 渲染体系恢复
- ✅ KaTeX WebView 创建（暂禁用）
- ✅ Prompt 补强（希腊字母、JSON 转义、方括号禁止）

### v51 (7deba03)
- ✅ `\triangle` 被 `_reNakedAngle` 误拆为 `\tri\angle` — 修正正则前瞻
- ✅ cases 环境 `\ ` 单反斜杠+空格 → `\\` 双反斜杠换行
- ✅ `_reBracketCases` 正则 `\b` 被解释为 word boundary — 修正
- ✅ 全量希腊字母支持（α β γ δ … Ω），`_supported` + `_symbols` 扩展
- ✅ `\text` 命令加入 `_supported` 白名单

### v52 (16f2941)
- ✅ 物理选项行尾反斜杠：JSON `\\\n` 解码为 `\`+LF，Step 0 清理
- ✅ 方程组 cases 在 `\([...]\)` 包裹下渲染失败：`\(` → `$` 与 `[...]` → `$$` 碰撞产生 `$$$`，新增 `_reDelimBracketCases` 一步到位处理

### v53 (3f042cb)
- ✅ cases 在 `\(...\)` 内联定界符中渲染失败：`\(` → `$` 后 cases 变成 `$...$`（内联），flutter_math_fork 无法渲染。新增 `_reInlineCases` 检测并升级为 `$$...$$`（display）
- ✅ 同时修复 analysisResult.steps 和 exercises 中的 cases 渲染
- ✅ 34 项单元测试全通过，129 项全量测试通过

### v54 (pending commit)
- ✅ 首页统计柱状图数据与标签错位：`StatsBarChart` 柱子 x 值硬编码 0/1/2 对应 mastered/reviewing/newQ，但 `MasteryLevel` 枚举顺序是 newQuestion(0)/reviewing(1)/mastered(2)，导致新增6题显示为"已掌握6"。改用 `MasteryLevel.xxx.index` 对齐。
- ✅ 129 项全量测试通过

## Blockers
- GitHub push 暂时失败（SSL 连接超时），commit 在本地，网络恢复后 `git push` 即可

## Next First Step
- 安装 v54 APK 真机验证：
  1. 首页统计柱状图是否正确（新增6应显示在"新增"柱子）
  2. 方程组大括号 cases 渲染（v54 包含所有最新代码，pipeline trace 输出正确）
- 如果 v54 通过，继续处理：
  - 举一反三退化问题（几何题 → 一元一次方程）
  - KaTeX WebView 性能优化
  - 文科拆题优化（plan 已就绪）

## APK 版本
- v50: ai-wrong-notebook-v50-20260430-1205.apk
- v51: ai-wrong-notebook-v51-20260430-latex-fix.apk
- v52: ai-wrong-notebook-v52-20260430-physics-fix.apk
- v53: ai-wrong-notebook-v53-20260430-cases-fix.apk
- v54: ai-wrong-notebook-v54-20260430-1802.apk

## 测试
- 129 tests passed ✅
